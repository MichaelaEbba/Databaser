1. Vart hittar man log-filerna för databaserna?
- mysql: var/log/mysql/ mongoDB: /var/log/mongodb/

2. Vad är skillnaden mellan en SQL databas som mysql och en nosql?
- Mysql är en relaionsdatabas, vilket nosql/mongodb inte är.

3. Vad är likheterna mellan mysql och nosql?
- CRUD används i båda. 

4. När skall man använda den ena eller den andra typen av databas?
- Vill man ha en databas med många relationer så använder man mysql. Vill du ha snabbhet och          skalbarhet så använder man mongodb.

5. Vad skall man tänka på om man ger någon annan tillgång till sin databas?
- Vad man sätter för behörigheter på nya användare.

6. Vad kan enligt GDPR vara känsligt att spara i en databas?
- Personliga uppgiter, hela namn, address, personnummer osv. 

7. Exempel på sql & nosql databaser.
- Maria DB, Oracle för MySQL. MongoDB och Amazon DynamoDB för noSQL. 

8.  Om flera företag, organisationer eller personer använder samma databas, varför är det viktigt med separata användare? 
- Sekretess mellan användarna, samt har man olika projekt så vill man hålla isär dom.

9. Vilka delar i CRUD gör/gör inte ändringar?
- CRUD står för create, read, update och delete. Det är bara "read" som inte gör några ändringar. 

10. Hur ansluter man till någon annans DB via terminalen?
- MongoDB = mongo --host mongodb0.example.com:28015 
- MySQL = mysql --host=localhost --user=myname --password=password mydb
